language: node_js
node_js:
  - '10'
  - '8'
cache: yarn
script:
  - yarn commitlint-travis
  - yarn build
  - yarn format:verify
  - yarn lint
  - yarn test
  - git diff --exit-code docs/
  - git diff --exit-code yarn.lock
